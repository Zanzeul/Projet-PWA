export interface TVShows {
    id: string;                // Identifiant unique de la série
    name: string;             // Titre de la série
    overview: string;          // Synopsis de la série
    releaseDate: string;       // Date de sortie au format ISO (ex: "2023-05-15")
    posterPath: string;        // URL ou chemin vers l'affiche de la série
}


